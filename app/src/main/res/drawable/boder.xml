<!-- res/drawable/rounded_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Màu nền -->
    <stroke
        android:width="1dp"
        android:color="@color/black"/> <!-- Màu và độ rộng viền -->
</shape>